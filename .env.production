# Client deployment branch (deploy/subiramaniyam-printing-works).
# Used by `vite build`; also mirrored in the Vercel project env.
# Supabase credentials are supplied by the Vercel project env, not committed here.
VITE_APP_NAME=Subiramaniyam Printing Works
